"use strict";

var _ = require("underscore");
var MonkeyBars = require("monkeybars");
var CreateObjectTask = require('./create_object_task').CreateObjectTask;

exports.CreateJsonTask = MonkeyBars.ParallelTask.extend({
	name: "CreateJsonTask",
	initialize: function(options) {

	},
	operate: function(data, task) {
		if(count > 1) {
			if(this.data === undefined) {
				this.data = [];
			}
			this.data.push(data);
		} else {
			this.data = data;
		}
	}
});